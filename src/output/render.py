# render.py — Step 8: Final structured output
# Input:  stitched tables + segmented document instances
# Output: final labels.json-compatible structured document
#         + before/after demo view (jumbled pages → clean instance list + stitched tables)
